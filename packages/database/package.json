{
  "name": "@ai-job-assistant/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo \"Database package uses Prisma generate, run: npm run db:generate\"",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "lint": "eslint prisma/",
    "test": "jest"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.8.1"
  }
}
